/**
 * Cypress E2E Support File
 *
 * This file is loaded automatically before every test file.
 * Used to configure global behaviors and custom commands.
 */
import "./commands";

/**
 * Global Cypress configuration
 */
beforeEach(() => {
  // Add configuration to run before each test
  // Examples: clear cookies, localStorage, etc.
});
