import AboutPage from '@/components/about/AboutPage'

/**
 * About page - server component for better SEO and performance
 * Contains static content that doesn't require client-side interactivity
 */
export default function About() {
  return <AboutPage />
}