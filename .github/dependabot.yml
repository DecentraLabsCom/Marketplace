# Dependabot configuration for automatic dependency updates
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # Monitor npm dependencies
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Los_Angeles"
    open-pull-requests-limit: 5
    # Add valid `assignees` or `labels` here if you want Dependabot to set them on PRs.
    versioning-strategy: increase
    # TypeScript and ESLint are constrained by the current toolchain;
    # review major upgrades manually after their peers add support.
    ignore:
      - dependency-name: "typescript"
        update-types:
          - "version-update:semver-major"
      - dependency-name: "eslint"
        update-types:
          - "version-update:semver-major"
    # Group non-major updates together
    groups:
      development-dependencies:
        patterns:
          - "@testing-library/*"
          - "jest*"
          - "eslint*"
          - "cypress"
        update-types:
          - "minor"
          - "patch"
      production-dependencies:
        patterns:
          - "next"
          - "react*"
          - "wagmi"
          - "@tanstack/*"
        update-types:
          - "patch"

  # Monitor GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Los_Angeles"
    open-pull-requests-limit: 3
    # Removed `dependencies` label to avoid Dependabot warnings about missing labels
    labels:
      - "github-actions"
